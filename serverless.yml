service: cache-service
frameworkVersion: '3'
plugins:
  - serverless-offline
  - serverless-python-requirements
custom:
  pythonRequirements:
    dockerImage: public.ecr.aws/shogo82148/lambda-python:3.11
    dockerizePip: false
    layer: true
    slim: true
    strip: false
    zip: true
functions:
  app:
    environment:
      APP_NAME: ${env:APP_NAME}
      APP_STAGE: ${env:STAGE}
      APP_TIMEZONE: ${env:APP_TIMEZONE}
      DEBUG: ${env:DEBUG}
      LOG_LEVEL: DEBUG
      POWERTOOLS_LOGGER_LOG_EVENT: true
      POWERTOOLS_METRICS_NAMESPACE: cache
      POWERTOOLS_SERVICE_NAME: cache-service
    events:
      - httpApi: '*'
    handler: app.main.handler
    layers:
      - arn:aws:lambda:${aws:region}:017000801446:layer:AWSLambdaPowertoolsPythonV2:40
      - Ref: PythonRequirementsLambdaLayer
package:
  patterns:
    - '!.git/**'
    - '!.gitignore'
    - '!.DS_Store'
    - '!npm-debug.log'
    - '!.serverless/**'
    - '!.serverless_plugins/**'
    - '!.venv/**'
  excludeDevDependencies: true
provider:
  name: aws
  region: eu-central-1
  runtime: python3.11
  stage: dev
  tracing:
    lambda: true
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:PutItem
            - dynamodb:Query
          Resource: ${env:AWS_ARN_DYNAMODB}
useDotenv: true
