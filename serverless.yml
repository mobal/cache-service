service: cache-service
frameworkVersion: '3'
plugins:
  - serverless-offline
  - serverless-python-requirements
custom:
  pythonRequirements:
    dockerImage: public.ecr.aws/shogo82148/lambda-python:3.10
    dockerizePip: false
    layer: true
    slim: true
    strip: false
    zip: true
functions:
  app:
    environment:
      APP_DEBUG: ${env:APP_DEBUG}
      APP_NAME: ${env:APP_NAME}
      APP_STAGE: ${env:APP_STAGE}
      APP_TIMEZONE: ${env:APP_TIMEZONE}
      LOG_LEVEL: DEBUG
      POWERTOOLS_LOGGER_LOG_EVENT: true
      POWERTOOLS_METRICS_NAMESPACE: cache
      POWERTOOLS_SERVICE_NAME: cache-service
    events:
      - httpApi: '*'
    handler: app.main.handler
    layers:
      - Ref: PythonRequirementsLambdaLayer
package:
  patterns:
    - '!.git/**'
    - '!.gitignore'
    - '!.DS_Store'
    - '!npm-debug.log'
    - '!.serverless/**'
    - '!.serverless_plugins/**'
    - '!.venv/**'
  excludeDevDependencies: true
provider:
  name: aws
  region: eu-central-1
  runtime: python3.10
  stage: dev
  tracing:
    lambda: true
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:PutItem
            - dynamodb:Query
          Resource: ${env:AWS_ARN_DYNAMODB}
useDotenv: true
